<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001">
  <AliasPackage>
    <AliasGroup isActive="yes" isFolder="yes">
      <name>g_maze solver</name>
      <script></script>
      <command></command>
      <packageName>g_maze solver</packageName>
      <regex></regex>

      <Alias isActive="yes" isFolder="no">
        <name>maze_help</name>
        <script>MazeShowHelp()</script>
        <command></command>
        <packageName></packageName>
        <regex>^#maze_help$</regex>
      </Alias>

      <Alias isActive="yes" isFolder="no">
        <name>maze_start</name>
        <script>StartSolver()</script>
        <command></command>
        <packageName></packageName>
        <regex>^#maze$</regex>
      </Alias>

      <Alias isActive="yes" isFolder="no">
        <name>maze_dump</name>
        <script>MazeDump()</script>
        <command></command>
        <packageName></packageName>
        <regex>^#maze_dump$</regex>
      </Alias>

      <Alias isActive="yes" isFolder="no">
        <name>maze_next</name>
        <script>MazeNextRoom()</script>
        <command></command>
        <packageName></packageName>
        <regex>^#maze_next$</regex>
      </Alias>

      <Alias isActive="yes" isFolder="no">
        <name>maze_next_dir</name>
        <script>MazeNextRoom_dir(matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^#maze_next_dir\s*([neswdu]?)$</regex>
      </Alias>

      <Alias isActive="yes" isFolder="no">
        <name>maze_goto</name>
        <script>MazeGotoRoom(matches[2])</script>
        <command></command>
        <packageName></packageName>
        <regex>^#maze_goto\s*(\d+)$</regex>
      </Alias>
    </AliasGroup>
  </AliasPackage>

  <ScriptPackage>
    <ScriptGroup isActive="yes" isFolder="yes">
      <name>g_maze solver</name>
      <script></script>
      <packageName>g_maze solver</packageName>

      <Script isActive="yes" isFolder="no">
        <name>maze_solver_main</name>
        <script><![CDATA[
G_MazeSolver = type(G_MazeSolver) == "table" and G_MazeSolver or {}
local M = G_MazeSolver

M.version = "1.1.1"
M.ms_basearea = M.ms_basearea or ""
M.ms_room_table = M.ms_room_table or {}
M.lastroom = M.lastroom or ""
M.lastmove = M.lastmove or ""
M.enablemap = M.enablemap or false
M.visited = M.visited or {}
M.handlers = M.handlers or {}

local function maze_note(msg)
  if type(cecho) == "function" then
    cecho(string.format("<black:beige>[MazeSolver] %s\n", tostring(msg)))
  elseif type(echo) == "function" then
    echo("[MazeSolver] " .. tostring(msg) .. "\n")
  end
end

local function gmcp_get(path)
  if type(gmcp) ~= "table" then return nil end
  local node = gmcp
  for part in string.gmatch(path, "[^.]+") do
    if type(node) ~= "table" then return nil end
    node = node[part]
    if node == nil then return nil end
  end
  return node
end

local function room_info()
  local info = gmcp_get("room.info") or gmcp_get("Room.Info")
  if type(info) ~= "table" then return nil end
  local zone = info.zone or info.Zone
  local num = info.num or info.vnum or info.VNum
  local exits = info.exits or info.Exits
  if zone == nil or num == nil or type(exits) ~= "table" then
    return nil
  end

  local normalized = {}
  for dir, val in pairs(exits) do
    normalized[tostring(dir)] = tostring(val)
  end

  return {
    zone = tostring(zone),
    num = tostring(num),
    name = tostring(info.name or ""),
    details = tostring(info.details or ""),
    exits = normalized,
  }
end

local function is_unmapped_exit(v)
  return tostring(v) == "-1"
end

function AddRoom(tt)
  M.ms_basearea = tt.zone
  M.lastroom = tt.num

  local troom = {
    num = tt.num,
    exits = tt.exits,
    area = tt.zone,
    name = tt.name,
  }

  table.insert(M.ms_room_table, troom)
  maze_note("New room in area " .. M.ms_basearea .. ": " .. troom.num .. " " .. troom.name)
end

function MazeDump()
  maze_note("Rooms in maze solver tables:")
  for _, room in ipairs(M.ms_room_table) do
    local x = string.format("Room: %s  %s, %s", room.num, room.area, room.name)
    for k, v in pairs(room.exits) do
      x = x .. string.format("   %s->%s", k, v)
    end
    maze_note(x)
  end
end

function MazeShowHelp()
  maze_note("----------------------------------------------------------------------------")
  maze_note("Maze solver v" .. (M.version or "1.1.1"))
  maze_note("#maze_help       - this info")
  maze_note("#maze            - clear Maze Solver tables")
  maze_note("#maze_dump       - display rooms visited so far and exits")
  maze_note("#maze_next       - try to go to next unmapped room")
  maze_note("#maze_goto <num> - go to room number <num>")
  maze_note("#maze_next_dir <e|s|w|u|d|n> - force specific direction")
  maze_note("----------------------------------------------------------------------------")
end

function StartSolver()
  local tt = room_info()
  if not tt then
    maze_note("No gmcp room.info available yet.")
    return
  end
  M.ms_room_table = {}
  AddRoom(tt)
end

function MazeNextRoom_dir(dir)
  dir = tostring(dir or "")
  if dir == "" then
    maze_note("Usage: #maze_next_dir <n|e|s|w|u|d>")
    return
  end

  local tt = room_info()
  if not tt then
    maze_note("No gmcp room.info available yet.")
    return
  end

  maze_note("Try to go " .. dir)
  M.lastroom = tt.num
  M.lastmove = dir
  M.enablemap = true
  send(dir)
end

local function dfs_find_path(from, dest, visited)
  local from_str = tostring(from)
  local dest_str = tostring(dest)
  visited[from_str] = true

  for _, room in ipairs(M.ms_room_table) do
    if room.num == from_str then
      for k, v in pairs(room.exits) do
        if tostring(v) == dest_str then
          return k
        end
      end

      for k, v in pairs(room.exits) do
        local vs = tostring(v)
        if not visited[vs] and not is_unmapped_exit(vs) then
          local path = dfs_find_path(vs, dest_str, visited)
          if path ~= "" then
            return k .. path
          end
        end
      end
      break
    end
  end

  return ""
end

function FindPathFromTo(from, dest)
  local visited = {}
  M.visited = visited
  return dfs_find_path(from, dest, visited)
end

function MazeGotoRoom(roomid)
  roomid = tostring(roomid or "")
  if roomid == "" then
    maze_note("Usage: #maze_goto <roomid>")
    return
  end

  M.visited = {}
  local tt = room_info()
  if not tt then
    maze_note("No gmcp room.info available yet.")
    return
  end

  local thisroom = tt.num
  if thisroom == roomid then
    maze_note("Already in room id: " .. thisroom)
    return
  end

  local path = FindPathFromTo(thisroom, roomid)
  maze_note("Executing path: " .. path)
  if path ~= "" then
    send("run " .. path)
  end
end

function MazeNextRoom()
  local tt = room_info()
  if not tt then
    maze_note("No gmcp room.info available yet.")
    return
  end

  local thisroom = tt.num
  local done = false
  local roomInTable = false

  for _, room in ipairs(M.ms_room_table) do
    if room.num == thisroom and room.area == tt.zone then
      for k, v in pairs(room.exits) do
        if is_unmapped_exit(v) then
          M.lastroom = thisroom
          M.lastmove = k
          M.enablemap = true
          send(k)
          done = true
          break
        end
      end
      roomInTable = true
      break
    end
  end

  if not roomInTable then
    AddRoom(tt)
    return
  end

  if done then return end

  maze_note("Room " .. thisroom .. " is fully mapped, trying first unmapped room")

  for _, room in ipairs(M.ms_room_table) do
    if room.area == tt.zone then
      for _, v in pairs(room.exits) do
        if is_unmapped_exit(v) then
          M.visited = {}
          local path = FindPathFromTo(thisroom, room.num)
          maze_note("Trying " .. room.num .. " path " .. path)
          if path ~= "" then
            send("run " .. path)
            return
          end
          break
        end
      end
    end
  end
end

function MazeSolver_OnRepop()
  local tt = room_info()
  if not tt then return end

  local removed = false
  for i = #M.ms_room_table, 1, -1 do
    if M.ms_room_table[i].area == tt.zone then
      table.remove(M.ms_room_table, i)
      removed = true
    end
  end

  if removed then
    maze_note("Some rooms were removed due to repop in area " .. tt.zone)
  end
end

function MazeSolver_OnRoomInfo()
  if not M.enablemap then return end

  local tt = room_info()
  if not tt then return end

  local thisroom = tt.num
  M.enablemap = false

  local roomIsMapped = false
  for _, room in ipairs(M.ms_room_table) do
    if room.num == M.lastroom then
      room.exits[M.lastmove] = thisroom
    end

    if room.num == thisroom then
      roomIsMapped = true
    end
  end

  if not roomIsMapped then
    AddRoom(tt)
  end
end

function M.shutdown()
  if type(killAnonymousEventHandler) == "function" then
    for _, id in ipairs(M.handlers) do
      pcall(killAnonymousEventHandler, id)
    end
  end
  M.handlers = {}
end

function MazeSolverInstall()
  M.shutdown()

  if type(registerAnonymousEventHandler) == "function" then
    table.insert(M.handlers, registerAnonymousEventHandler("gmcp.room.info", "MazeSolver_OnRoomInfo"))
    table.insert(M.handlers, registerAnonymousEventHandler("gmcp.Room.Info", "MazeSolver_OnRoomInfo"))
    table.insert(M.handlers, registerAnonymousEventHandler("gmcp.comm.repop", "MazeSolver_OnRepop"))
    table.insert(M.handlers, registerAnonymousEventHandler("gmcp.Comm.Repop", "MazeSolver_OnRepop"))
  end

  MazeShowHelp()
end

MazeSolverInstall()
        ]]></script>
      </Script>

      <Script isActive="yes" isFolder="no">
        <name>MazeSolverMCheckBridge</name>
        <packageName>g_maze solver</packageName>
        <script><![CDATA[
local function maze_solver_mcheck_state_file()
  local home = type(getMudletHomeDir) == "function" and getMudletHomeDir() or "."
  local dir = home .. "/persistence"
  if type(mkdir) == "function" then pcall(mkdir, dir) end
  if type(lfs) == "table" and type(lfs.mkdir) == "function" then pcall(lfs.mkdir, dir) end
  return dir .. "/mcheck_g_maze_solver_path.lua"
end

local function maze_solver_mcheck_load_path()
  local ok, saved = pcall(dofile, maze_solver_mcheck_state_file())
  return ok and type(saved) == "string" and saved or ""
end

local function maze_solver_mcheck_save_path(path)
  local file = io.open(maze_solver_mcheck_state_file(), "w")
  if not file then return end
  file:write("return " .. string.format("%q", path))
  file:close()
end

G_MazeSolver.mcheck = {
  protocol = "1",
  addon_id = "g_maze_solver",
  package_name = "g_maze solver",
  repository_id = "noobgonewild-mudlet-scripts",
  filename = "g_maze solver.xml",
  source_path = maze_solver_mcheck_load_path(),
}

function MazeSolverMCheckBridge(event_name, ...)
  local args = {...}
  local metadata = type(G_MazeSolver) == "table" and G_MazeSolver.mcheck or nil
  if not metadata then return end
  if event_name == "mcheck.scan.request" then
    local scan_id, protocol, requested_addon_id = args[1], tostring(args[2] or ""), tostring(args[3] or "")
    if protocol ~= metadata.protocol then return end
    if requested_addon_id ~= "" and requested_addon_id ~= metadata.addon_id then return end
    raiseEvent("mcheck.scan.response", tostring(scan_id or ""), metadata.protocol, metadata.addon_id,
      metadata.package_name, G_MazeSolver.version, metadata.repository_id, metadata.filename, metadata.source_path or "")
  elseif event_name == "sysInstallPackage" then
    local package_name, source_path = tostring(args[1] or ""), tostring(args[2] or "")
    if package_name == metadata.package_name and source_path ~= "" then
      metadata.source_path = source_path
      maze_solver_mcheck_save_path(source_path)
    end
  elseif event_name == "sysUninstallPackage" and tostring(args[1] or "") == metadata.package_name then
    if type(G_MazeSolver.shutdown) == "function" then pcall(G_MazeSolver.shutdown) end
  end
end
        ]]></script>
        <eventHandlerList>
          <string>mcheck.scan.request</string>
          <string>sysInstallPackage</string>
          <string>sysUninstallPackage</string>
        </eventHandlerList>
      </Script>
    </ScriptGroup>
  </ScriptPackage>
</MudletPackage>
